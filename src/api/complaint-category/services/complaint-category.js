'use strict';

/**
 * complaint-category service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::complaint-category.complaint-category');
