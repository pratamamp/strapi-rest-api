'use strict';

/**
 * complaint-category router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::complaint-category.complaint-category');
